import App from "./App";
import renderer from 'react-test-renderer'
import React from "react";

/*Testing with jest 
* Learn to use jest with current react-native code 
* "renderer Fix"
*/

it('works', () => {
    expect(1).toBe(1);
  });

 